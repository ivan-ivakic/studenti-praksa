var express = require('express');
var app = express();

app.use(express.static(__dirname + '/node_modules/hexis-commons/font-icon-picker/'));
app.use(express.static(__dirname + '/node_modules/hexis-commons/font-icon-picker/css/'));
app.use(express.static(__dirname + '/node_modules/hexis-commons/font-icon-picker/scripts/'));

// app.get('/project-icons.json', function (req, res) {
//   res.sendFile(__dirname + '/../../icons.json');
// });

app.get('/', function (req, res) {
  res.sendFile(__dirname + '/node_modules/hexis-commons/font-icon-picker/index.html');
});

var server = app.listen(3000, function () {

  // var host = 'localhost';
  // var port = 3000;

  console.log('Node server listening on port 3000');

});
